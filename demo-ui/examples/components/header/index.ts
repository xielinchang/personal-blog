import { withInstall } from '../../assets/utils/install';

import Header from './src/header.vue'

export const JHeader = withInstall(Header)

export default JHeader

// export type MainInstance = InstanceType<typeof Header>